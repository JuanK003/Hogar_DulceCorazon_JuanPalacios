@model IEnumerable<Hogar_Dulce_Corazón____Juan_Palacios.Models.Adoption_Detail>

@{
    ViewBag.Title = "Index";
}

<style>
    a {
        color: aliceblue;
    }

        a:hover {
            color: aliceblue
        }
</style>

<br />
<br />
<br />
<br />
<br />

<h2>Agregar</h2>

<p class="btn btn-primary btn-lg">
    @Html.ActionLink("Create New", "Create")
</p>

<br />
<br />

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Moving_Date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Adoption_Date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Return_Date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Children.Name)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Moving_Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Adoption_Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Return_Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Children.Name)
            </td>
            <td>
                <p class="btn btn-primary btn-lg">
                    @Html.ActionLink("Edit", "Edit", new { id = item.ID_AdoptionDetail })
                </p>
                <p class="btn btn-primary btn-lg">
                    @Html.ActionLink("Details", "Details", new { id = item.ID_AdoptionDetail })
                </p>
                <p class="btn btn-primary btn-lg">
                    @Html.ActionLink("Delete", "Delete", new { id = item.ID_AdoptionDetail })
                </p>
            </td>
        </tr>
    }

</table>